import RegisterForm from '@/components/auth/RegisterForm';

const RegisterPage = () => {
    return (
        <div className="min-h-screen flex items-center justify-center p-4 bg-gradient-to-br from-primary-600 via-purple-600 to-secondary-600">
            <RegisterForm />
        </div>
    );
};

export default RegisterPage;
